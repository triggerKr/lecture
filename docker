

전체 프로세스를 
[파일 업로드] → [도커 파일 준비] → [실행] 순서로 깔끔한 정리.

1단계: 로컬 폴더를 우분투 어디?
    1) 우분투 서버의 추천 경로
    우분투 리눅스에서 일반 사용자가 가장 자유롭게 권한 제약 없이 쓸 수 있는 공간은 홈 디렉토리(~/) 내부입니다. 내일 접속할 계정 이름이 ubuntu라고 가정하면, 아래 경로에 작업 폴더생성.

    추천 경로: /home/ubuntu/apps/ (또는 간단히 ~/apps/)

    2) 폴더를 업로드하는 가장 쉬운 방법 (GUI 도구 사용)
    터미널 명령어로 업로드하는 것보다 윈도우 화면에서 드래그 앤 드롭.
    (FileZilla, MobaXterm ) SFTP 

    서버에 원격 접속.
    /home/ubuntu/ 경로 아래에 apps라는 폴더생성.
    로컬 폴더들을 통째로 선택해 apps 폴더 안으로 드래그 앤 드롭.

2단계: 도커를 사용하기 위한 파일 준비 (각 폴더별 세팅)
    폴더 업로드가 끝났다면, 이제 리눅스 터미널(SSH)에 접속해서 도커를 태울 준비를 합니다.

    1) 각 마이크로서비스 폴더마다 Dockerfile 만들기
    각 imap-* 폴더 내부에 확장자 없이 Dockerfile이라는 이름으로 파일을 하나씩 생성 후 
    아래 내용 작성.

        Dockerfile
        # 1. 파이썬 실행 환경 베이스 지정
        FROM python:3.11-slim

        # 2. 컨테이너 내부의 작업 디렉토리 설정
        WORKDIR /app

        # 3. 라이브러리 목록 복사 및 설치 (가상환경 필요 없음, 도커 자체가 격리 공간)
        COPY requirements.txt .
        RUN pip install --no-cache-dir -r requirements.txt

        # 4. 소스 코드 전체 복사
        COPY . .

        # 5. 프로그램 실행 (FastAPI/Uvicorn 예시 - 포트는 서비스마다 다르게 설정)
        EXPOSE 8000
        CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
        💡 핵심 포인트: 도커 컨테이너 자체가 완전히 격리된 룸이기 때문에, 도커 내부에서는 .venv를 만들 필요가 없습니다! pip install을 하면 도커 룸 안에 알아서 깔끔하게 설치됩니다.

    2) 최상위 apps 폴더에 docker-compose.yml 생성.
    모든 imap-* 폴더들이 모여있는 /home/ubuntu/apps/ 바로 아래에 
    총지휘관 파일인 docker-compose.yml을 생성.

        YAML
        version: '3.8'

        services:
        # 1. 인증 서비스
        auth-service:
            build: ./imap-auth          # imap-auth 폴더에 있는 Dockerfile을 읽어서 빌드해라
            container_name: imap-auth
            ports:
            - "8000:8000"             # 외부포트:컨테이너내부포트
            restart: always             # 프로그램이 죽으면 도커가 알아서 자동 재시작!

        # 2. 주문 서비스
        order-service:
            build: ./imap-order         # imap-order 폴더에 있는 Dockerfile을 읽어서 빌드해라
            container_name: imap-order
            ports:
            - "8001:8001"
            restart: always


3단계: 도커로 마이크로서비스 일괄 실행
        모든 파일 준비가 끝났다면, 터미널에서 /home/ubuntu/apps/ 경로로 이동한 뒤 
        아래 명령어 딱 하나만 실행.

        Bash
        # 이미지 빌드 및 백그라운드 가동
        docker compose up -d --build

🌟 이 시점부터 개발자님이 누리게 될 도커의 강력한 장점들:
1. 배치 파일 무용지물 (자동 관리): 이제 폴더마다 들어가서 start.bat를 일일이 누를 필요가 없습니다. docker compose up 명령 하나로 5개든 10개든 마이크로서비스가 한 번에 다 켜집니다.

2. 완벽한 백그라운드 운영 (-d 옵션): 터미널 창을 닫아도 서비스들은 리눅스 백그라운드에서 안전하게 계속 돌아갑니다. nohup 같은 복잡한 리눅스 명령어를 따로 공부하실 필요가 없습니다.

3. 모니터링의 단순화: 어떤 서비스가 잘 돌고 있고 어떤 포트를 쓰는지 한눈에 보고 싶다면 아래 명령어 하나로 끝납니다.

        Bash
        docker compose ps

4. 실시간 로그 통합 확인: 특정 마이크로서비스에서 에러가 나는지 실시간으로 관찰하고 싶다면 로그 명령어를 쓰시면 됩니다.

        Bash
        # 인증 서비스의 프린트문/로그 실시간 확인
        docker compose logs -f auth-service
        
5.  **무중단 부분 업데이트:** 나중에 `imap-order` 서비스의 코드만 수정해서 서버에 다시 올렸다면, 전체를 껐다 켤 필요 없이 해당 서비스만 아래 명령어로 깔끔하게 재빌드할 수 있습니다.
    ```bash
    docker compose up -d --build order-service


====================================================================





나는 지금 리눅스 우분투 서버에 내가 현재 로컬에 가지고 있는 파이썬 마이크로 서비스들을 올려서 운영 해 보려 해. 일단 텅빈 VM 을 배당 받긴 했는데, 거기에 MS를 올리려고 하는데 가이드좀 해줘.

물론입니다, 태원님.

지금부터는 "개발 PC에 있는 Python Micro Service들을 Ubuntu VM에 배포해서 운영하는 환경"을 구축하는 과정이라고 생각하면 됩니다.

태원님이 앞으로 하시려는 프로젝트가 삼성 디지털트윈(iMAP 계열)처럼 여러 Python MS가 각각 독립적으로 돌아가는 구조라면, 처음부터 운영환경을 잘 만들어두는 것이 굉장히 중요합니다.

제가 추천하는 방식은 Docker 없이 Linux Native 환경 기준으로 설명드리겠습니다. (나중에 Docker로 변경하는 것도 가능합니다.)

전체 구조

예를 들어 현재 로컬이

imap-auth
imap-dna
imap-parser
imap-worker

이렇게 있다고 가정하겠습니다.

Ubuntu 서버에서는 이렇게 구성하는 것을 추천합니다.

/opt/imap/
    imap-auth/
        .venv/
        app.py
        requirements.txt
        logs/
        config/

    imap-dna/
        .venv/
        ...

    imap-parser/
        .venv/
        ...

    imap-worker/
        .venv/
        ...

즉

서비스 하나
=
폴더 하나
=
venv 하나

입니다.

현재 운영하는 방식과 거의 동일합니다.

1단계 Ubuntu 기본 세팅

먼저 접속

ssh user@server-ip

업데이트

sudo apt update
sudo apt upgrade -y

필수 프로그램 설치

sudo apt install -y

git

python3

python3-pip

python3-venv

build-essential

curl

wget

vim

버전 확인

python3 --version
pip3 --version
2단계 프로젝트 폴더 생성
sudo mkdir -p /opt/imap

권한

sudo chown $USER:$USER /opt/imap

이후

cd /opt/imap
3단계 소스 올리기

방법은 여러가지입니다.

가장 추천

Git

git clone

또는

scp

scp -r imap-auth user@server:/opt/imap/

또는

WinSCP

또는

VSCode Remote SSH

4단계 venv 생성

예)

cd /opt/imap/imap-auth

python3 -m venv .venv

활성화

source .venv/bin/activate

확인

which python

결과

/opt/imap/imap-auth/.venv/bin/python
5단계 라이브러리 설치
pip install -r requirements.txt

또는

pip install fastapi
pip install uvicorn
...
6단계 실행 확인

예)

python app.py

또는

uvicorn main:app

브라우저에서

http://IP:8000

확인

7단계 방화벽

Ubuntu에는

ufw

가 있습니다.

확인

sudo ufw status

8000 포트 허용

sudo ufw allow 8000
8단계 백그라운드 실행

처음에는

nohup

으로도 됩니다.

예)

nohup python app.py &

하지만 운영환경에서는 비추천입니다.

9단계 systemd 서비스 등록 (강력 추천)

예를 들어

imap-auth

서비스를 등록합니다.

sudo vim /etc/systemd/system/imap-auth.service

내용

[Unit]
Description=imap-auth
After=network.target

[Service]

User=ubuntu

WorkingDirectory=/opt/imap/imap-auth

ExecStart=/opt/imap/imap-auth/.venv/bin/python app.py

Restart=always

RestartSec=5

[Install]
WantedBy=multi-user.target

저장 후

sudo systemctl daemon-reload

서비스 등록

sudo systemctl enable imap-auth

시작

sudo systemctl start imap-auth

확인

sudo systemctl status imap-auth
10단계 로그 확인
journalctl -u imap-auth -f

실시간 로그가 출력됩니다.

11단계 서버 재부팅 테스트
sudo reboot

재접속

systemctl status imap-auth

자동 실행되면 성공입니다.

운영환경에서는 추가하면 좋은 것들
nginx

여러 Micro Service 앞에 Reverse Proxy

Client
      ↓
Nginx
      ↓
8001 auth
8002 parser
8003 dna
8004 worker
HTTPS
Let's Encrypt

적용

Supervisor

예전에는 많이 사용했지만

요즘은 대부분

systemd

를 사용합니다.

환경변수
.env

관리

예)

DB_URL=

JWT_KEY=

REDIS_HOST=
로그 폴더
logs/
auth.log

worker.log

parser.log
태원님 프로젝트라면 제가 추천하는 최종 운영 구조
Ubuntu VM

/opt/imap
    imap-auth
        .venv
        logs
        config
        requirements.txt

    imap-dna
        .venv
        logs

    imap-parser
        .venv
        logs

    imap-worker
        .venv
        logs

/etc/systemd/system
    imap-auth.service
    imap-dna.service
    imap-parser.service
    imap-worker.service

Nginx

    80
    443

        ↓

    auth :8001

    dna :8002

    parser :8003

    worker :8004

제가 추천드리는 방식은 개발 → 테스트 → 운영 순서로 진행하는 것입니다.

Python 및 필수 패키지 설치
프로젝트 복사 및 .venv 구성
서비스 단독 실행 확인
systemd로 서비스 등록
nginx Reverse Proxy 구성
HTTPS 및 도메인 적용
Git 기반 CI/CD(예: GitHub Actions 또는 Jenkins)로 자동 배포

태원님이 이전에 말씀하셨던 VM 기반 CI/CD 자동화까지 고려한다면, 이 구조가 가장 확장성과 운영성이 좋습니다. 이후 Git에 푸시하면 VM이 자동으로 소스를 받아 가상환경을 갱신하고 서비스를 재시작하는 형태로 자연스럽게 발전시킬 수 있습니다.


=========================================






그러면 해당 반영 MS들을 .venv를 쓰지 않고 각각을 도커로 생성한다면, 다시 가이드 부탁해

좋습니다, 태원님.

오히려 지금 시점이라면 .venv 방식보다 Docker 방식을 추천드립니다.

특히 태원님 프로젝트가

Python Micro Service 여러 개
VM에서 운영
나중에 CI/CD 구축 예정
서비스마다 사용하는 라이브러리가 다를 수 있음

이라면 Docker가 훨씬 관리하기 쉽습니다.

전체 아키텍처

기존에는

Ubuntu
 ├── imap-auth
 │    └── .venv
 ├── imap-dna
 │    └── .venv
 ├── imap-parser
 │    └── .venv

였다면

Docker에서는

Ubuntu
    │
Docker Engine
    │
    ├── Container (imap-auth)
    │       Python
    │       FastAPI
    │       requirements
    │
    ├── Container (imap-dna)
    │       Python
    │
    ├── Container (imap-parser)
    │       Python
    │
    └── Container (imap-worker)

입니다.

즉,

venv 하나 = Docker Container 하나

라고 생각하면 이해가 쉽습니다.

1. Ubuntu에 Docker 설치
sudo apt update

sudo apt install docker.io docker-compose-v2 -y

자동 시작

sudo systemctl enable docker
sudo systemctl start docker

확인

docker --version
docker compose version
2. 프로젝트 구조

추천 구조입니다.

/opt/imap

    docker-compose.yml

    imap-auth/
        app.py
        requirements.txt
        Dockerfile

    imap-dna/
        app.py
        requirements.txt
        Dockerfile

    imap-parser/
        app.py
        requirements.txt
        Dockerfile

    imap-worker/
        app.py
        requirements.txt
        Dockerfile

여기서 중요한 것은

각 Micro Service마다 Dockerfile이 하나씩 존재하는 것입니다.

3. Dockerfile 만들기

예를 들어

imap-auth

FROM python:3.12-slim

WORKDIR /app

COPY requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

COPY . .

EXPOSE 8001

CMD ["python", "app.py"]

끝입니다.

venv가 전혀 필요 없습니다.

왜냐하면

Container 자체가
독립된 Python 환경

이기 때문입니다.

4. requirements.txt

예)

fastapi
uvicorn
redis
requests
numpy
pandas

컨테이너 생성 시 자동 설치됩니다.

5. docker-compose 작성

가장 중요한 파일입니다.

version: '3.9'

services:

  auth:
    build: ./imap-auth
    container_name: imap-auth
    ports:
      - "8001:8001"
    restart: always

  dna:
    build: ./imap-dna
    container_name: imap-dna
    ports:
      - "8002:8002"
    restart: always

  parser:
    build: ./imap-parser
    container_name: imap-parser
    ports:
      - "8003:8003"
    restart: always

  worker:
    build: ./imap-worker
    container_name: imap-worker
    ports:
      - "8004:8004"
    restart: always
6. 실행
cd /opt/imap

docker compose up -d

이 한 줄이면

Dockerfile 빌드

↓

Python 설치

↓

requirements 설치

↓

컨테이너 생성

↓

자동 실행

모두 끝납니다.

7. 확인
docker ps
imap-auth

imap-dna

imap-parser

imap-worker

가 실행 중이면 성공입니다.

8. 로그 확인

예)

docker logs imap-auth

실시간

docker logs -f imap-auth
9. 수정 후 재배포

예를 들어

auth 수정

cd /opt/imap

docker compose build auth

docker compose up -d auth

또는

docker compose up -d --build auth
10. 업데이트

Git Pull

git pull

재빌드

docker compose up -d --build

끝입니다.

11. 서비스 간 통신

이게 Docker의 장점입니다.

예를 들어

auth에서

requests.get("http://imap-dna:8002/status")

처럼 호출하고 싶을 수 있는데, container_name이 항상 DNS 이름으로 동작하는 것은 아닙니다. 같은 Docker Compose 네트워크에서는 서비스 이름을 사용하는 것이 권장됩니다.

즉 docker-compose.yml에서 서비스 이름이 dna라면

requests.get("http://dna:8002/status")

처럼 호출하면 됩니다.

Docker가 내부 DNS를 자동으로 제공합니다.

12. 환경 변수

예)

services:

  auth:

    environment:

      DB_HOST=db

      JWT_SECRET=1234

Python

import os

secret = os.getenv("JWT_SECRET")
13. 데이터 유지

로그

DB

파일

등은

volumes:

    - ./logs:/logs

처럼 Volume으로 연결합니다.

예)

Container

    /logs

        ↓

Ubuntu

/opt/imap/logs

컨테이너를 삭제해도 로그가 남습니다.

14. nginx

운영에서는 거의 필수입니다.

인터넷

      │

    Nginx

      │

 ┌────┴────┐

8001

8002

8003

8004

클라이언트는

https://server/api/auth

https://server/api/parser

https://server/api/dna

처럼 접속하고,

Nginx가 각각의 컨테이너로 프록시합니다.

15. CI/CD

나중에는

Git Push

↓

GitHub Action

↓

SSH

↓

git pull

↓

docker compose up -d --build

↓

배포 완료

이 구조가 가장 깔끔합니다.

제가 추천하는 최종 운영 구조
Ubuntu VM
│
├── Docker Engine
│
├── docker-compose.yml
│
├── imap-auth
│      ├── Dockerfile
│      ├── requirements.txt
│      └── app.py
│
├── imap-dna
│      ├── Dockerfile
│      ├── requirements.txt
│      └── app.py
│
├── imap-parser
│
├── imap-worker
│
├── logs/
│
└── nginx
태원님 프로젝트에서는 한 가지 더 추천드립니다.

현재 구조가 imap-auth, imap-dna, imap-parser, imap-worker처럼 여러 Python MS를 함께 운영하는 플랫폼이라면, docker-compose.yml 하나에 모든 서비스를 정의하는 것도 좋지만, 각 서비스마다 독립적인 docker-compose.yml을 두고 상위 디렉터리에서 관리하는 방식도 고려해볼 만합니다. 이렇게 하면 특정 서비스만 독립적으로 배포하거나 롤백하기가 쉬워지고, 나중에 서비스 수가 늘어나도 관리가 편해집니다.

다음 단계에서는 "실제 삼성 프로젝트 수준"을 가정해서 다음 요소까지 포함한 운영 구조를 함께 설계해 드릴 수 있습니다.

Docker + Nginx + Redis + PostgreSQL
공통 네트워크 구성
.env를 이용한 환경별 설정(dev/stage/prod)
GitHub Actions 또는 Jenkins 기반 무중단 CI/CD
헬스체크와 자동 재시작 정책
로그 수집(예: Loki, ELK 등)까지 고려한 운영 아키텍처



=================================================


"실제 삼성 프로젝트 수준"을 가정해서 다음 요소까지 포함한 운영 구조를 설계 해줘

좋습니다, 태원님.

태원님이 지금 진행하는 프로젝트와 삼성 프로젝트의 성격(디지털 트윈, Python MS, SpringBoot, Vue, CI/CD)을 종합해서 보면, 저는 중견 SI가 아니라 대기업 수준의 운영 아키텍처를 기준으로 설계하는 것을 추천드립니다.

제가 삼성 프로젝트를 한다면 아래와 같이 설계합니다.

전체 아키텍처
                               ┌──────────────────────────────┐
                               │        GitHub / GitLab       │
                               └──────────────┬───────────────┘
                                              │
                                        Git Push
                                              │
                                    GitHub Actions
                                    (CI/CD Pipeline)
                                              │
                           ┌──────────────────┴──────────────────┐
                           │                                     │
                     Build Docker Image                  Run Unit Test
                           │
                    Push Image Registry
                           │
          ┌────────────────┴────────────────┐
          │                                 │
          │          Ubuntu VM              │
          │                                 │
          │  Docker Engine                  │
          │                                 │
          │  ┌─────────────────────────┐    │
Internet──┼─▶│        Nginx            │    │
          │  │ Reverse Proxy + HTTPS   │    │
          │  └─────────────┬───────────┘    │
          │                │                │
          │        Docker Network           │
          │                │                │
          │ ┌──────────────┼──────────────────────────┐
          │ │              │                          │
          │ ▼              ▼                          ▼
          │ auth         dna                      parser
          │ 8001         8002                     8003
          │
          │ worker
          │ 8004
          │
          │ redis
          │
          │ postgres
          │
          │ minio
          │
          │ prometheus
          │
          │ grafana
          │
          │ loki
          │
          └─────────────────────────────────────────────┘

이게 실제 운영 환경과 거의 동일한 구조입니다.

디렉토리 구조
/opt/imap

    docker-compose.yml

    .env

    nginx/

    auth/

        Dockerfile

        src/

        requirements.txt

    dna/

        Dockerfile

        src/

    parser/

        Dockerfile

    worker/

        Dockerfile

    redis/

    postgres/

    minio/

    monitoring/

        prometheus.yml

        grafana/

    logs/

    backup/

이렇게 프로젝트를 구성합니다.

Docker Compose

실제로는 하나의 Compose에서 모든 서비스를 관리합니다.

services

    nginx

    auth

    dna

    parser

    worker

    redis

    postgres

    minio

    prometheus

    grafana

    loki

서비스 수가 20~30개가 되어도 관리하기 쉽습니다.

Docker Network

모든 서비스는 내부 네트워크에서만 통신합니다.

               Docker Network

        auth
           │
           │
        parser
           │
           │
        dna
           │
           │
        worker
           │
           │
        redis
           │
           │
        postgres

Python에서는

requests.get("http://auth:8001/login")

처럼 서비스 이름으로 호출하면 됩니다.

IP 주소를 전혀 사용할 필요가 없습니다.

Reverse Proxy

외부에서는

https://server/api/auth

https://server/api/dna

https://server/api/parser

만 보입니다.

Nginx가

/api/auth

↓

auth:8001

/api/parser

↓

parser:8003

로 전달합니다.

따라서 외부에서는 Docker 구조를 알 수 없습니다.

HTTPS
Client

↓

443

↓

Nginx

↓

Let's Encrypt

SSL은 Nginx만 가지고 있습니다.

Python 서비스는 HTTP만 사용합니다.

Redis

공통 캐시

JWT BlackList

Session

Message Queue

Cache

예를 들어

redis.set(...)

하면

모든 MS가 공유 가능합니다.

PostgreSQL

공통 DB

auth

↓

postgres

dna

↓

postgres

worker

↓

postgres

DB Connection Pool도 구성합니다.

MinIO

파일 저장

Image

Simulation

CSV

Report

AI Model

모두 MinIO에 저장합니다.

실제 삼성 프로젝트에서도 S3 호환 스토리지를 많이 사용합니다.

로그

각 서비스가

stdout

으로만 로그를 출력하면 됩니다.

Docker가

Container Log

↓

Loki

↓

Grafana

로 수집합니다.

따라서

Python에서

logging

만 사용하면 됩니다.

모니터링

Prometheus

↓

Grafana

모니터링 항목

CPU

Memory

Network

Disk

Container Status

Response Time

API Count

Error Count

실시간으로 볼 수 있습니다.

환경 변수

절대 코드에 넣지 않습니다.

.env

예

DB_HOST=postgres

DB_PORT=5432

JWT_SECRET=xxxx

REDIS_HOST=redis

MINIO_URL=minio

LOG_LEVEL=INFO

Python

os.getenv("JWT_SECRET")
Health Check

모든 MS는

GET

/health

를 제공합니다.

예

{
    "status":"UP"
}

Docker

Nginx

Prometheus

모두 이 API를 사용합니다.

인증

추천 구조

Vue

↓

Nginx

↓

Auth MS

↓

JWT

↓

다른 MS

각 MS는

JWT만 검증합니다.

로그인은

Auth 서비스만 수행합니다.

CI/CD

이 부분이 가장 중요합니다.

개발자

↓

Git Push

↓

GitHub Actions

↓

Docker Build

↓

Docker Image 생성

↓

Docker Registry Push

↓

VM 접속

↓

docker compose pull

↓

docker compose up -d

↓

배포 완료

운영자는

SSH 접속도 거의 하지 않습니다.

무중단 배포(Blue-Green)

현재

auth_v1

운영 중

새 버전

auth_v2

생성

Nginx

v1

↓

v2

로 변경

기존 컨테이너 종료

사용자는

배포를 느끼지 못합니다.

백업

매일 새벽

Postgres Dump

↓

backup/

↓

NAS

↓

S3

자동 백업합니다.

보안

운영에서는 다음을 적용합니다.

외부 공개 포트는 80, 443만 개방
PostgreSQL, Redis, MinIO는 내부 Docker Network에서만 접근
JWT Secret과 DB 비밀번호는 .env 또는 Docker Secret으로 관리
Nginx에서 Rate Limiting과 CORS 적용
Spring Boot나 Python 서비스는 내부 포트만 노출
최종 운영 구조
Ubuntu Server
│
├── Docker Engine
│
├── Docker Compose
│
├── Docker Network
│
├── Nginx
│
├── Auth MS
│
├── DNA MS
│
├── Parser MS
│
├── Worker MS
│
├── Redis
│
├── PostgreSQL
│
├── MinIO
│
├── Prometheus
│
├── Grafana
│
├── Loki
│
├── GitHub Actions (배포)
│
└── Backup
태원님 프로젝트에 맞춘 추가 제안

태원님이 이전에 설명해 주신 프로젝트를 종합해 보면,

Vue: 사용자 UI
Spring Boot: 인증, 인가, API Gateway 역할 및 일부 비즈니스 로직
Python Micro Service: 디지털 트윈 계산, Modelica 연동, AI/데이터 처리
PostgreSQL: 업무 데이터 저장
Redis: 캐시 및 작업 상태 공유
MinIO: 시뮬레이션 결과, 모델 파일, 업로드 파일 저장
Nginx: Reverse Proxy 및 HTTPS
Docker Compose: 운영 환경 오케스트레이션
GitHub Actions/Jenkins: CI/CD 자동화
Prometheus + Grafana + Loki: 모니터링 및 로그 통합

이 구성이 현재 삼성과 같은 대규모 제조/디지털 트윈 프로젝트에서도 충분히 통할 수준의 표준 아키텍처입니다. 향후 서비스가 늘어나거나 VM이 여러 대로 확장될 경우에도, 이 구조는 자연스럽게 Kubernetes 같은
환경으로 이전할 수 있는 기반이 됩니다.
